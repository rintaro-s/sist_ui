#!/bin/bash
# --- Sist OS マスターセッションスクリプト ---
# この単一のスクリプトが、デスクトップ環境のすべてを起動します。

# Openboxウィンドウマネージャをバックグラウンドで起動します。
# --config-file オプションで、私たちのカスタム設定を強制的に読み込ませます。
openbox --config-file ~/.config/openbox/rc.xml &

# Flutter UIをメインアプリケーションとして起動します。
# 'exec' コマンドは、このアプリが終了するとセッション全体も終了することを意味します。
exec /opt/sist_ui/sist_ui