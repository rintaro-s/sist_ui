#!/bin/bash
# --- 最終診断用 autostart ---
# Flutterアプリの代わりに、xtermだけを起動してみる

# 診断用のログファイル
LOG_FILE="/tmp/diagnostic_autostart.log"
exec > $LOG_FILE 2>&1
set -x

# xtermをセッションのメインプロセスとして起動する
# このウィンドウを閉じるとセッションが終了する
exec xterm